import './../../scss/app.scss'

import React from 'react';
import PropTypes from 'prop-types';

// views

const App = props => {

  return (
    <div className = 'app'>
      <p>This is the app</p>
    </div>
  );
};

App.propTypes = {
}

export default App;
